import 'package:flutter/material.dart';

class VideoScreen extends StatelessWidget {
static final String path="VideoScreen"; 
  const VideoScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}